package families.features;

import superclasses.features.Feature;

public class FamilyFeature extends Feature {
	public FamilyFeature(int id, String featureName, String featureData) {
		super(id, featureName, featureData);
	}
}